// 变量声明和赋值
就是 数字 = 42
就是 文本 = "VScript"
就是 布尔值 = 真

// 算术运算
就是 结果 = (数字 + 8) * 2
输出(结果)  // 输出 100

// 条件语句
如果 (结果 > 50) {
    输出("结果大于 50")
} 否则 {
    输出("结果小于或等于 50")
}

// 循环
对于 (i 在 范围(1, 5)) {
    输出("第 " + i + " 次循环")
}

// 数组
就是 数组 = [1, 2, 3, 4, 5]
输出("数组长度：" + 长度(数组))

// 类型检查
输出("数字的类型：" + 类型(数字))
输出("文本的类型：" + 类型(文本))
输出("数组的类型：" + 类型(数组))
